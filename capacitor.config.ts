import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.balegariew.app',
  appName: 'balegariew',
  webDir: 'out',
  bundledWebRuntime: false
};

export default config;
